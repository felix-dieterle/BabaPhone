{
    "name": "felix-dieterle/babaphone-backend",
    "description": "BabaPhone Backend API for mobile data mode",
    "type": "project",
    "require": {
        "php": ">=7.4"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.5"
    },
    "scripts": {
        "test": "phpunit --colors=always",
        "test-coverage": "phpunit --coverage-html coverage"
    }
}
